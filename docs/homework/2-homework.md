# Домашнее задание
## Производительность индексов

### Цель
В результате выполнения ДЗ вы воспользуетесь/создадите набор тестовых данных для проведения нагрузочного тестирования, подберете наиболее подходящие индексы и проведете тесты производительности.

### Тренируемые навыки
- *(опционально)* генерация тестовых данных
- работа с индексами
- нагрузочное тестирование

---

### Описание / Пошаговая инструкция

1. **Сгенерировать** любым способом **1,000,000 анкет**. Имена и Фамилии должны быть реальными, чтобы учитывать селективность индекса. Так же можно воспользовать уже готовым списком как основой.
2. **Реализовать функционал поиска** анкет по префиксу имени и фамилии (одновременно) в вашей социальной сети (реализовать метод `/user/search` из спецификации). Запрос в форме `firstName LIKE ? AND secondName LIKE ?`. Сортировать вывод по `id` анкеты.
3. **Провести нагрузочные тесты** этого метода. Поиграть с количеством одновременных запросов: **1 / 10 / 100 / 1000**.
4. **Построить графики** и сохранить их в отчет.
5. **Сделать подходящий индекс.**
6. **Повторить пункт 3 и 4.**
7. **Предоставить отчет**, в котором должны быть:
   - графики **latency** до индекса
   - графики **throughput** до индекса
   - графики **latency** после индекса
   - графики **throughput** после индекса
   - запрос добавления индекса
   - `EXPLAIN` запросов после индекса
   - объяснение почему индекс именно такой

> ДЗ принимается в виде отчета по выполненной работе.

---

### Критерии оценки
Оценка происходит по принципу **зачет / незачет**.

### Требования
- Правильно выбраны индексы
- Нагрузочное тестирование проведено и результаты адекватны
- В случае генерации тестовых данных самостоятельно, убедиться что эти данные "похожи" на настоящие

### Компетенции
**Работа с высоконагруженными БД:**
- работать с индексами
- оптимизировать запросы к БД
- осуществлять профилирование запросов

---

## Чеклист выполнения

- [x] **Генерация тестовых данных** — реализован `POST /test-data/generate` (контроллер + сервис + валидации + реалистичные русские имена/фамилии/города)
- [x] **Реализовать `/user/search`** — поиск по префиксу имени и фамилии (`LIKE`), сортировка по `id`
- [x] **Prometheus + Actuator** — метрики HTTP, HikariCP, JVM
- [x] **Grafana** — установлен (`brew install grafana`)
- [ ] **Нагрузочное тестирование БЕЗ индекса** — прогнать с 1/10/100/1000 одновременных запросов
- [ ] **Графики latency + throughput БЕЗ индекса** — сохранить в отчет
- [ ] **Создать индекс** — подобрать подходящий индекс для запроса поиска
- [ ] **Нагрузочное тестирование С индексом** — повторить тесты
- [ ] **Графики latency + throughput С индексом** — сохранить в отчет
- [ ] **EXPLAIN запросов** после добавления индекса
- [ ] **Написать объяснение** почему выбран именно такой индекс
- [ ] **Оформить отчет**
